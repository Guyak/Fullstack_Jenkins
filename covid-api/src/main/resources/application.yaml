spring:
  datasource:
    url: ${DB_COVID_API}
    username: root
    password: root
  jpa:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      open-in-view: false
      show-sql: true
      hibernate.ddl-auto: update
  liquibase:
    enabled: false
logging:
  level:
    org:
      springframework:
        security: DEBUG
